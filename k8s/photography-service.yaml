apiVersion: v1
kind: Service
metadata:
  name: photography
  namespace: hallphotography
  annotations:
    traefik.ingress.kubernetes.io/service.serversscheme: https
spec:
  selector:
    app: photography
  ports:
  - port: 8080
    targetPort: 8080
  type: ClusterIP